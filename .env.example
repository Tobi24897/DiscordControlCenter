# Discord user token (Discord in the browser -> F12 -> Network -> any request to discord.com -> Request Header "Authorization")
DISCORD_TOKEN=

# Local web server port
SERVER_PORT=8020

# Seconds between poll cycles (also editable live in the Settings UI)
POLL_INTERVAL=15

# 1 = do not open the browser automatically on start
NO_BROWSER=
